<h1>Log a new Plank</h1>

<%= form_for @student_plank, :url => student_planks_path do |f| %>
  <%= f.label :plank %>
  <%= f.collection_select :plank_id, Plank.all, :id, :planktype %>
  <br>
  <%= f.label :duration %>
  <%= f.text_field :duration %>
  <br>
  <%= f.submit %>
  <% end %>
